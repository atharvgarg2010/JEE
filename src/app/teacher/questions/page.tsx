import { QuestionsListClient } from "@/components/questions/questions-list-client";

export const metadata = {
  title: "Question Bank | JEE Tracker",
};

export default function TeacherQuestionsPage() {
  return <QuestionsListClient />;
}
